runner('routers.hash.without-prefix.without-anchor test suite', describe, it, __dirname, async ($, page) => {
    /* const helper = async expectedValue => {
        const url = page.url(), hash = url.substring(url.indexOf('#') + 1);
        expect(hash).toBe(expectedValue);
        expect(await $('#routerPath').text()).toBe(expectedValue);
    };
    await helper('default-router');
    await $('#button1').click();
    await helper('index1');
    await $('#button2').click();
    await helper('index2'); */
});
